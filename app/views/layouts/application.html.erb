<!DOCTYPE html>
<html>
  <head>
    <title>MedicalDirectoryFinalRailsProject</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <i><h1>Ben's Medical Directory</h1></i>
    <% if logged_in? %>
      - Account:
      <% if current_user.role_type == "Doctor" %>
        <%= link_to current_user.email, specialty_doctor_path(current_user.role.specialty, current_user.role) %>
      <% else %>
        <%= link_to current_user.email, patient_path(current_user.role) %>
      <% end %>
      <br>
    <% end %>
    ________________________________________________________________
    <br>
    <br>
    <%= yield %>
  </body>
  <footer>
    <p>
      <br>
      _____________________________________________________________________________
      <br>
      <%= link_to "Home", root_path %> | <%= link_to "About", about_path %><% if logged_in? %>
         | <%= link_to "Log Out", logout_path, method: "delete" %>
      <% end %>
       | &copy Ben's Medirect 2018
    </p>
  </footer>
</html>
